This is a website used for calculating statistics associated with tennis matches.

It uses the django web framework. 

The tool stack used was a python (django) backend connected to a MySQL database with html and CSS being used on the frontend.
